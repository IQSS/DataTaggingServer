package models

/**
  * Used to transfer data about customizations to and from the UI.
  */
case class CustomizationDTO(
                           frontPageText:String,
                           parentProjectLink:String,
                           parentProjectText:String
                           )
